// let chooseimg = function (id, event) {
//     let output = $(id);
//     output.attr('src', URL.createObjectURL(event.target.files[0]));
//     output.on('load', function() {
//         URL.revokeObjectURL(output.attr('src'));
//     });
// }
